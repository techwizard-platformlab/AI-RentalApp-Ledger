apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: api-gateway-vs
  namespace: rental-dev
spec:
  hosts:
    - "*"
  gateways:
    - rental-gateway
  http:
    - match:
        - uri:
            prefix: /api/
        - uri:
            prefix: /admin/
        - uri:
            exact: /healthz
      route:
        - destination:
            host: api-gateway.rental-dev.svc.cluster.local
            port:
              number: 8000
          weight: 100

---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: api-gateway-internal-vs
  namespace: rental-dev
spec:
  hosts:
    - api-gateway.rental-dev.svc.cluster.local
  gateways:
    - mesh
  http:
    - route:
        - destination:
            host: api-gateway.rental-dev.svc.cluster.local
            port:
              number: 8000
          weight: 100
