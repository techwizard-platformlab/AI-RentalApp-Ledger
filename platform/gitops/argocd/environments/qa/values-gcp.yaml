# QA environment overrides — GCP
global:
  repoURL: "https://github.com/YOUR_ORG/YOUR_REPO"
  targetRevision: main

app:
  name: rental-ledger
  namespace: rental-qa
  environment: qa
  cloud: gcp
  project: rental-ledger
  manifestPath: "platform/kubernetes/overlays/qa"
  registryServer: "REGISTRY_URL"
  sync:
    automated: false
    prune: false
    selfHeal: false
    createNamespace: true
  revisionHistoryLimit: 10

appProject:
  name: rental-ledger
  description: "Rental App Ledger — qa (GCP)"
  sourceRepos:
    - "https://github.com/YOUR_ORG/YOUR_REPO"
  destinations:
    - server: "https://kubernetes.default.svc"
      namespace: "rental-qa"
  clusterResourceWhitelist:
    - group: ""
      kind: Namespace
    - group: rbac.authorization.k8s.io
      kind: ClusterRoleBinding
  namespaceResourceBlacklist:
    - group: ""
      kind: ResourceQuota
