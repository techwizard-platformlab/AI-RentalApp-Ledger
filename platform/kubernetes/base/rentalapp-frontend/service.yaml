apiVersion: v1
kind: Service
metadata:
  name: rentalapp-frontend
  labels:
    app: rentalapp-frontend
    project: rentalAppLedger
spec:
  selector:
    app: rentalapp-frontend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
