apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: rental-dev
  labels:
    app: notification-service
    project: rentalAppLedger
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - name: http
      port: 8003
      targetPort: 8003
      protocol: TCP
